<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!-- Cast SDK entry point (full flavor only): names the OptionsProvider the framework
             instantiates to configure the Chromecast receiver. -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.micasong.player.CastOptionsProvider" />
    </application>
</manifest>
